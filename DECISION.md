# DECISION — round1: LEFM 초보자용 설명 단일 HTML 문서

작성: Claude Code / 날짜: 2026-09-04
입력: `PROBLEM.md`, `round1/claude.md` + `round1/lefm_초보자_설명.html`(Claude 산출물),
`round1/codex.md`(Codex 산출물, HTML을 .md에 인라인), `round1/claude_review.md`, `round1/codex_review.md`
출처: `J:\Desktop\선행연구 관련\` 의 [선행연구 1](Kwon 2021, JPVT 143, 064503),
[선행연구 3](Kwon·Markoff·DeFisher 2024, Materials 17, 569) — 두 PDF 본문만.

---

## 1. 공통 의견 (양측 일치 + 양쪽 리뷰가 PDF 대조로 확인)

두 산출물 사이에 **사실관계 정면충돌은 없다.** 인용한 식·문장은 양쪽 다 두 PDF와 일치한다.

| # | 합의 내용 | 근거 |
|---|---|---|
| 1 | 출처 제한 준수 — 두 PDF만 사용, `K=σ√(πa)`·Griffith 유도 등 교과서 내용 배제 | [선1] References는 Irwin·Kirsch·Sapora 3건뿐(Griffith 없음) |
| 2 | 단일 HTML, 외부 의존 0 (CDN·이미지 파일·라이브러리 없음) | Claude: canvas+JS / Codex: 인라인 SVG |
| 3 | `σ_x = K/√(2πr)` — [선1] Eq.(4), Irwin(1957) 근접장 | [선1] p.2 원문 일치 |
| 4 | 균열선단 응력 → 무한대(특이성) | [선1] Case 2 "stress at the crack tip is infinite" / [선3] §1 |
| 5 | 선단에서는 응력조건이 자동 만족 → 응력구배(또는 K)가 파손 지배 | [선1] Case 2 / [선3] §4.3 |
| 6 | `K_Ic = √(2πE·w_f)`, `w_f` 단위 J/m² | [선1] Eq.(7) + 본문 단위 서술 |
| 7 | 파손 판정 = K 대 K_Ic. `K < K_Ic` 미도달 / `K ≥ K_Ic` Mode I 진전 개시 | [선3] Eq.(4) "Y ... equivalent to the critical energy release rate" |
| 8 | 구멍·둔한 노치에는 균열의 특이장과 같은 판정을 그대로 쓰지 않음 | [선3] §1 |
| 9 | [선3] §4.3: K는 2D FEA + Eq.(3) 곡선맞춤으로 구함, 파손하중은 Eq.(4)로 | [선3] p.7 |
| 10 | 그림은 원문 이미지 복제가 아니라 새로 그림 | 양쪽 명시 |

**두 리뷰가 독립적으로 같은 결론에 도달한 항목**(아래 C2·C3·C6)은 신뢰도가 높아 그대로 채택했다.

---

## 2. 핵심 충돌 / 강조점 차이

### C1. 시각화 방식 — 가장 큰 차이
- **Claude**: `σ_x=K/√(2πr)` 를 실제로 계산해 그린 정량 그래프 3개 (선형축 발산 곡선 /
  log–log 기울기 −1/2 / K–K_Ic 판정 + 하중단계 막대) + `console.assert` self-check.
- **Codex**: 계산 없는 개념 도식 2개. 응력 곡선은 실제 식이 아니라 손으로 그린 베지어 path.
  K–K_Ic **게이지**는 초보자에게 직관적.
- **판정: 정량 표현은 Claude가 충실.** PROBLEM의 "균열선단 응력집중 시각화" 요건상 발산·−1/2
  기울기가 눈에 보여야 한다. Codex 문서에서는 이를 볼 수 없다. 반면 Codex의 SVG는 JS 없이도
  보이고 `<title>/<desc>` 접근성이 있다 → **Claude 그래프를 유지하되, JS-off 대체 텍스트와
  `aria-label`을 canvas에 추가**(반영 완료).

### C2. `w_f` 를 "에너지방출률"로 단정 (Claude HTML의 과장)
- Claude HTML 원문: "`w_f`의 단위가 J/m² — 즉 단위면적당 에너지(에너지방출률) 차원이다".
- **양쪽 리뷰 모두 지적**: [선1]은 `w_f` 를 응력구배 조건에서 나온 파손 임계값으로 도입하고
  그 **차원이** 에너지/면적과 같다고만 한다. 차원 일치를 물리량의 동일성으로 확대하면 안 됨.
- **판정: "에너지방출률과 같은 차원"으로 수정.** 임계 에너지방출률과의 명시적 등가성은
  [선3]의 `Y` 에만 있다(원문 "equivalent to the critical energy release rate"). (반영 완료)

### C3. 출처 밖 기호 `G_c` 도입 (Claude HTML)
- Claude HTML 2곳에서 `G_c` 사용. Claude 스스로 "Griffith/에너지평형은 본문에 없음"이라 밝힘.
- **codex_review 지적**: 엄격한 "두 PDF만" 조건에서는 `Y` 또는 논문이 직접 쓴 표현에 머물러야 함.
- **판정: `G_c` 전부 제거, `Y` 로 통일.** (반영 완료)

### C4. 두 논문의 2π 계수 불일치
- **Claude**: [선1] `w_f=K²/(2πE)` vs [선3] `Y=K²/E` → 2π(≈6.28)배 차이라고 경고 박스로 명시.
- **Codex**: "상수 표기가 다르니 섞지 말라"는 한 줄, [선3] Eq.(4) 자체를 문서에 안 실음.
- **판정: Claude의 경고 박스 유지가 옳다.** 실재하는 계수 불일치이고(관례상 `G=K²/E'` 이므로
  [선3] 쪽이 표준 정규화), 초보자용이라도 이 함정은 짚을 가치가 있다. 다만 원인을 "K 정규화의
  차이"로 **단정**하지 않고, [선3] Eq.(3)의 K가 별도 정의 없이 쓰였다는 사실만 서술 →
  HTML 내부 모순(위는 K²/(2πE), 아래는 "두 논문 모두 K²/E")을 제거하고 두 식을 구분해 표기.
  (반영 완료)

### C5. σ_l(local stress) vs σ_e(effective stress) 개념 뭉갬 (Claude 자기지적)
- Claude HTML은 [선1] `σ_l` 과 [선3] `σ_e` 를 "σ_l ≥ σ_f" 로 통합.
- **claude_review 지적**: [선3]의 `σ_e` 는 재료거동별로 다르게 정의되고(취성=최대주응력 등),
  [선3]이 연성·이방성·복합재로 확장되는 근거가 이 개념인데 "국부응력"으로만 부르면 사라짐.
- **판정: §2에 `σ_e`(유효응력) 구분 문단 추가, 요약표에서도 분리.** (반영 완료)

### C6. 출처에 없는 재료명 (Claude HTML)
- 본문 "유리·세라믹·경화 시멘트·PMMA", JS 주석 "(PMMA 급)" — [선3] §4.1의 경화 시멘트
  페이스트만 두 PDF에 실재. 나머지 재료명은 뒷받침 없음.
- **양쪽 리뷰 지적**. **판정: 두 논문이 든 예(경화 시멘트 페이스트)로 한정, "(PMMA 급)" 주석 삭제.**
  (반영 완료)

### C7. [선3] §1 "fracture mechanics is suitable for holes" 문구
- PDF에 실제로 그렇게 인쇄돼 있으나 앞뒤 논리("entirely different criteria")상 "not"의 누락으로 보임.
- **판정: 그 문구를 직접 인용하지 않고 논문의 문맥적 취지(구멍엔 별도 기준)만 서술.** Claude·Codex
  둘 다 이미 이 방식 → 유지. "구멍엔 부적합"을 LEFM 전체의 절대적 정의로 쓰지 않고
  "이 두 논문의 K–K_Ic 판정은 날카로운 균열에 적용"으로 한정. (반영 완료)

### C8. Claude 문서 완성도 vs Codex 문서 완성도
- Codex: sticky 목차·헤더 등 레이아웃 완성도가 높음.
- Claude: 수식 요약표, 응력구배 식, 자료의 한계 박스, 연성재 한계.
- **판정: 내용 범위(Claude)가 초보 학습 문서에 더 중요. 레이아웃은 현재 Claude HTML도 충분히
  가독성 있음 → 골격 유지.**

### C9. 문서 문서화 오류: "self-check 3개"
- `claude.md`는 "console.assert 3개"라 했으나 HTML엔 2개뿐(codex_review 지적, Codex가 실제 실행해 확인).
- **판정: 실질 오류 아님(문서 설명이 코드와 불일치했을 뿐). log–log 기울기 −1/2 검증
  assert를 하나 추가해 3개로 맞춤.** (반영 완료)

---

## 3. 판단 근거 요약

- 두 PDF는 `J:\Desktop\선행연구 관련\` 에 실재. claude_review는 PDF 직접 열람,
  codex_review는 `pdftotext` 재추출 + Claude HTML의 `<script>` 를 Node vm에서 실제 실행
  (`JS_EXEC_OK 2 assertions`, 예외 없음) — **두 검증이 서로 일치**.
- 하드 팩트 오류는 양측 모두 없음. 남은 문제는 **Claude HTML의 과잉 단정 4건**(C2 `w_f`=에너지방출률,
  C3 `G_c`, C5 σ_l/σ_e, C6 재료명)과 **Codex 시각화의 정성성**(C1).
- 최종 산출물은 **Claude HTML을 골격으로**(실제 `.html` 파일 존재, 정량 시각화, self-check)
  C2–C7·C9의 수정을 반영하고, Codex의 강점(JS-off 대체 텍스트·접근성)을 canvas에 흡수하는 것으로 결정.
- Codex HTML을 버리는 것이 아니라, 두 리뷰가 합의한 수정 목록을 Claude HTML에 적용하는 것이
  가장 적은 작업으로 두 답의 검증된 장점을 모두 담는 길이다.

---

## 4. 최종 산출물

- **`round1/lefm_초보자_설명.html`** (수정 완료) — 단일 HTML, 외부 의존 0.
  - §1 정의 → §2 강도기준의 한계(σ_l / σ_e 구분) → §3 균열선단 응력집중 + K + 응력구배
    (그림 1: 발산 곡선, 그림 2: log–log −1/2) → §4 K 대 K_Ic 파손 판정(그림 3) + 2π 경고 박스
    → §5 적용 범위·한계 → §6 수식 요약표 → §7 결론.
  - 그림 3개: 문서 내부 `<canvas>` + 순수 JS(계산 그래프). 각 canvas에 `aria-label` +
    JS-off 대체 텍스트. `<script>` 끝 `console.assert` 3개.

## 5. 미해결 / 후속

1. 브라우저 렌더링 육안 확인은 두 세션 모두 못 함(브라우저 표면 없음). canvas 텍스트 좌표
   겹침 가능성은 실제로 열어봐야 확정 — 사용자/메인 세션에서 파일을 열어 확인 필요.
2. 2π 계수 불일치의 원인은 지정 2개 PDF만으로 확정 불가. Irwin(1957) 원문이 필요하면 별도 요청.
3. 예시 수치(K=1 MPa√m, E=3 GPa, σ_f=70 MPa, K_Ic=1 MPa√m)는 곡선 형태 재현용 임의값 —
   실제 재료값으로 해석 금지(HTML에 명시됨).
