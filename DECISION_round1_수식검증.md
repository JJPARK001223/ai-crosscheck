# DECISION — Round 1: 선행연구 1·3 수식 유도 검증 (최종 종합)

작성: Claude (판사 역할, 규칙 #20). 근거: `round1/claude.md` (Claude blind) + `round1/codex.md` (Codex blind).
두 검증은 서로의 답을 보지 않고 각자 원문 PDF·페이지 이미지를 읽고 손으로 유도했다.

> ⚠️ 참고: 이 라운드 진행 중 다른 세션이 `PROBLEM.md`를 "round2"(HCP FE 모델링 인용 검증)로
> 덮어썼다. 이 라운드의 원 문제는 커밋 `51302e8` 및 `round1/claude.md`·`round1/codex.md`에 보존됨.

---

## 1. 두 검증이 **일치**한 결론 (신뢰도 높음)

| 항목 | 결론 | Claude | Codex |
|---|---|:--:|:--:|
| 선1 식(2)→(3) | 대수 유도 정확. 분수 정리 → `wf = (σ³/2E)|ds/dσ|` | ✔ | ✔ |
| 선1 Case 2: 식(4)→(5) | `\|dσx/dy\| = K/(2√(2π) y^{3/2})` 정확 | ✔ | ✔ |
| 선1 Case 2: 식(6) | `wf = K²/(2πE)` — 식(3)에 대입 시 r 소거되어 정확 | ✔ | ✔ |
| 선1 Case 2: 식(7) | `K_IC = √(2πE·wf)` — 식(6)의 역산, 정확 | ✔ | ✔ |
| 선1 Case 3: `σθ(R,π/2)=3σo` | 식(9)에서 정확 (Kirsch SCF = 3) | ✔ | ✔ |
| 선1 Case 3: 식(11) | `\|∂σθ/∂r(R,π/2)\| = 7σo/R` — 미분 정확 | ✔ | ✔ |
| 선1 Case 3: 식(12) | `3σo ≥ σf → σo ≥ σf/3` 정확 | ✔ | ✔ |
| 선3 식(3)→(4) | `Y = K²/E` — 논문 표기 안에서 대수 정확 (s 소거) | ✔ | ✔ |
| 선1 wf ≡ 선3 Y | **같은 재료상수의 재표기.** 부등식 재배열하면 정의가 일치 | ✔ | ✔ |
| 선3 "Y = 임계 에너지방출률" 주장 | **느슨함/부정확.** 논문 자신의 식(3)(2π 없음)을 일관 적용하면 `Y = G/(2π)` (평면응력) | ✔ | ✔ |
| 2π 차이의 정체 | 두 재료상수의 물리적 차이가 아니라 **응력장 계수 K의 정규화 차이**를 같은 기호로 쓴 것 | ✔ | ✔ |
| Fig 8·9 시편·경계조건 | 반쪽(대칭) 3D 모델, H·L/2·W, 하단 대칭면 균열 깊이 a, 롤러 지지 + 중앙 상면 분포하중. L/H=4, W=100mm, E=20.8 GPa. 원문과 일치 | ✔ | ✔ |
| Fig 10 K 추출 방법의 **원리** | FEA 응력을 `σ=K/√s`에 곡선맞춤해 단위하중당 K를 얻는 발상 자체는 타당 (선형 → K∝P) | ✔ | ✔ |
| SENB 3점굽힘 표준 K 핸드북식 | `K_I = (PS/BH^{3/2})·f(a/H)`, S/H=4. FEA 표준 K의 개념적 교차검산에만 사용 가능 | ✔ | ✔ |

**→ 선행연구 1의 모든 수식 유도(식 2·3·5·6·7, 식 9→3σo, 식 11·12·13)는 수학적으로 정확하다.**
**→ 선행연구 3의 식(3)→식(4) 유도도 논문 표기 안에서 정확하다.**

---

## 2. Codex가 **추가로** 지적한 점 (Claude 검토 후 수용)

1. **선1 식(3)의 적용 한계** — 식(3)의 등식은 (i) 파괴 임계상태에서만, (ii) `dσ/ds ≠ 0`인 구간에서만
   성립한다. 균일응력장(Case 1)에는 직접 적용 불가 → Case 1은 식(1)만으로 처리하는 것이 맞다.
   *수용: 타당한 명확화.*

2. **선1 Case 3의 "dominant" 표현은 느슨함** — 논문은 `σo ≥ σf/3`이면 식(13)이 "지배"한다고 하나,
   이는 자동으로 보장되지 않는다. 실제 지배조건은 두 임계 공칭응력의 크기 비교이다:
   $$\sigma_{o,c} = \max\!\left(\frac{\sigma_f}{3},\ \sqrt{\frac{14\,E\,w_f}{27R}}\right)$$
   (뒤 항은 식(13)에 `σθ=3σo`, `|∂σθ/∂r|=7σo/R` 대입해 Codex가 유도. Claude 재검산 결과 일치:
   `(3σo)³/(2E·7σo/R) = 27Rσo²/14E ≥ wf` → `σo ≥ √(14Ewf/27R)`.)
   *수용: 유도 정확. 논문 서술의 부정확성이며 최종식이 틀린 것은 아님.*

3. **선3 식(2)의 인쇄 결함** — 원문 식(2) `σe ≥ (2EY(dσe/ds))^{1/3}`에 절댓값 표시가 없다.
   균열 전방에서 `dσe/ds < 0`이므로 우변이 음수가 되어 조건이 무의미하게 항상 만족된다.
   이후 유도(식 3→4)는 암묵적으로 `|dσe/ds|`를 쓴다. **의도는 명확하나 표기 오류.**
   선1 식(2)에는 절댓값이 명시되어 있어, 선3에서 누락된 것으로 판단.
   *수용: 표기 결함. 선1이 올바른 형태.*

4. **평면응력/평면변형률 구분 누락** — `Y = wf`를 Irwin `G`와 연결할 때:
   - 평면응력: `Y = wf = G/(2π)` (여기서 `G = K_I²/E`)
   - 평면변형률: `Y = wf = K_I²/(2πE) = G/(2π(1−ν²))` (여기서 `G = K_I²(1−ν²)/E`)
   두 논문 모두 `E'` 구분 없이 `E`만 쓴다. 시멘트 페이스트(두께 W=100mm, 관통균열)는
   평면변형률에 가까우므로 `ν ≈ 0.2` 가정 시 약 4% 차이. *수용: 정밀도 관점의 지적.*

5. **Fig 10 K 추출의 재현성 공백 (Codex)** — 논문 보고만으로는 추출된 K를 독립 재현 불가:
   - Fig 10 가로축이 "정규화 거리"인데 식(3)의 `s`는 길이 차원 필요 → 정규화 길이 `ℓ₀`와
     역변환(`×√ℓ₀`)이 보고되지 않음.
   - K-지배영역의 맞춤 구간, 제외한 선단 근방 절점 수, 적합도, 구간 민감도 미보고.
   - quarter-point 특이요소·J-적분·interaction integral과의 교차검산 없음.
   - "약 10,000 요소" + 정성적 언급만으로 K 수렴 입증 불가.
   - Y를 단 1개 시험(H=100mm, a/H=0.1)에서 역식별 → 그 점의 "정확한 일치"는 **검증이 아니라 보정**.
     나머지 형상 예측만이 독립 검증 자료.
   *수용: Claude도 항목 7에서 유사 지적(1개 시편 오차 전파). Codex 쪽이 더 구체적.*

---

## 3. Claude ↔ Codex **판정 차이** (충돌로 기록, 억지 통합 안 함)

| 쟁점 | Claude | Codex | 판사 판정 |
|---|---|---|---|
| Fig 8·9 K 추출 절차의 종합 평가 | "타당" | "원리는 타당하나 보고·검증 불충분, 정량적 타당성 입증 안 됨" | **둘 다 맞음, 초점이 다름.** *방법론*은 건전(둘 다 동의). *논문의 서술 완결성*은 부족(Codex 지적이 옳음 — 독자가 K값을 재현할 수 없음). 최종 표현: "방법은 건전, 논문 보고는 불완전." |
| 선3 식(2) 절댓값 | 있는 것으로 간주하고 유도 | "인쇄 결함"으로 명시 | **Codex 채택.** 원문 스캔(p.2) 확인 결과 절댓값 없음이 맞다. |

---

## 4. 최종 판정 (판사)

### 4.1 수식 유도 자체
- **선행연구 1: 식(1)–(13) 전 유도가 수학적으로 정확.** 재현 검산 통과.
- **선행연구 3: 식(1)–(4) 유도가 논문 표기 안에서 정확.** 식(5)–(9)(다중스케일)은 유도가
  원문에서 생략되어 이 라운드 범위 밖 (근거: [40] Kwon&Park 2013, [41] Kwon&Darcy 2018).

### 4.2 해석·서술의 느슨한 점 (3가지, 모두 결과값 자체는 안 틀림)
1. **선3 "Y = 임계 에너지방출률(G_c)" 문장은 수치적으로 부정확.** 일관되게 보면 `Y = G/(2π)`
   (평면응력). 실무 영향 없음 — Y를 시험에서 역추출하므로 정규화 상수가 상쇄. 단 **"Y에
   문헌상 G_c 값을 그대로 대입"하면 2π배(≈6.28배) 오차** → 절대 그렇게 쓰면 안 됨.
2. **선1 Case 3 "식(13)이 지배" 서술은 조건부.** 실제 지배조건은
   `σo,c = max(σf/3, √(14Ewf/27R))`.
3. **선3 식(2) 절댓값 누락** (인쇄 결함). 선1 식(2)가 올바른 형태 `|dσe/ds|`.

### 4.3 선행연구 3의 재현성 (석사 연구에서 이 논문을 재현할 경우)
- Fig 8·9 시편: `L/H = 4`, `W = 100 mm`, `E = 20.8 GPa`, `a/H = 0.1/0.3/0.5`, `H = 50·100 mm`.
  절대치수(H, L, a)는 논문에 없음 → 채택값 필요. 실험 데이터 원출처는
  **Karihaloo, Abdalla, Xiao, "Size effect in concrete beams," EFM 2003, 70, 979–993** (ref [39]).
- K 추출: FEA 응력–거리 데이터를 `σ = K/√s`에 곡선맞춤. **논문의 비표준 K 정의**를 그대로
  쓰거나(그러면 `Y = K²/E`), 표준 `K_I`로 환산해 쓰되(`K_I = √(2π)·K_논문`) 그때는
  `Y = K_I²/(2πE)`를 써야 함. **두 관례를 섞지 말 것.**
- 표준 SENB 핸드북식으로 FEA K를 교차검산 권장:
  `K_I/P = (S/BH^{3/2})·f(a/H)`, `f(α) = 3√α[1.99 − α(1−α)(2.15 − 3.93α + 2.7α²)] / [2(1+2α)(1−α)^{3/2}]`.

---

## 5. 산출물에 반영할 사항
`(클로드 & 코덱스 검증파일)/선행연구1_3_검증분석/` 의 5개 HTML:
- 완역 2개: 선3 §2에 "Y = G_c 주장은 느슨함(= G/2π)" 번역 주 삽입 (완료).
- 수식유도 2개: 위 4.1의 단계별 유도 + 4.2의 3가지 느슨한 점 명시.
- Fig 8·9 해설: 4.3의 시편·경계조건·K 추출·SENB 교차검산식.
