# DECISION — round10: 발표 pptx `2026_09_07.pptx` 오류 검토 (Claude 최종)

Claude blind: `round10/claude.md` · Codex blind: `round10/codex.md`
원문 대조: 선1 = *JPVT* 2021, 143, 064503 (p.-1~2) · 선3 = *Materials* 2024, 17, 569 (p.2, 6~8)

---

## 1. 공통 결론 (Claude·Codex 일치)

| ID | 슬라이드 | 오류 | 정정 |
|---|---|---|---|
| **D-01** | 4 (통합기준 식) | 계수 `2KY` | → `2EY`. 선3 식(2) "E is the modulus", 선1 식(2)(3)(13) 모두 E. **차원으로도** `2KYσ'` = Pa³√m ≠ Pa³ 이므로 이중으로 오류 (Codex R10-09). 부수: `dσ_e/d_s` → `dσ_e/ds`, 절댓값 `|dσ_e/ds|` 표기. |
| **D-02** | 6 (Q 분해, image7) | `σ_e³/2E` 를 "변형 에너지 밀도 U" 로 명명 | 지수 3 → 2. 에너지밀도 `U = σ_e²/(2E)` [Pa]. 같은 슬라이드 image8/9 식은 정확. 두 번째 행 아래첨자 `1/σ_E` → `σ_e` 통일 (Codex R10-10). `P_a·m` → `Pa·m`. |
| **D-03** | 8·9 | 오타 | `CMZ`→`CZM`, `Tcd`→`TCD`, `Grifith`→`Griffith`, `lrwin`→`Irwin`. |
| **D-04** | 12 | `Karihallo` | → `Karihaloo`. 정식: B. L. Karihaloo, H. M. Abdalla, Q. Z. Xiao, "Size effect in concrete beams", *Eng. Fract. Mech.* **70**, 979–993 (2003). 선3 ref [39]. 연도 2003 정확. |
| **D-05** | 5 | "방향별 유효응력" (이방성 갈래) | 선3 p.2 원문엔 있으나 round9에서 이방성·PLA·GFC·고분자 제거 지시. 발표 범위에서 삭제 권장. ("준취성" ≠ 이방성·고분자, 혼동 금지.) |
| **D-06** | 11 식번호 | 선1 식(6)(7), 선3 식(4) | **인용 정확**. 단 임계값 표기는 `K_IC`(선1)·`A_c` 또는 "파괴 개시 시 K"(선3)로 통일 필요. |
| **D-07** | 12·13 시편·절차 | — | W=100·L/H=4 고정, H 2 × a/H 3 = 6, E=20.8 GPa, Y 1회 역산 절차 — 선3 §4 와 **일치, 오류 아님**. 단 "시편 6개" → "6개 형상 케이스"(D-12). |

---

## 2. 판정 차이 — Claude 최종 결정

### D-08 [슬라이드 11] "같은 물리량의 다른 기호 표기" 프레이밍
- **Claude blind (C10-02)**: 두 값이 2π배 다르므로 "같은 물리량"은 틀림 → "정의값이 2π 다름"으로 고쳐라.
- **Codex 이견 (R10-02)**: "2π 다르다"도 **틀린 결론**. 두 논문의 `K` 는 같은 응력장을 정의하지 않는다.
  같은 물리적 균열장을 같은 위치에서 비교하면 `A(선3) = K_I(선1)/√(2π)` → `Y = A_c²/E = K_IC²/(2πE) = w_f`.
  **즉 Y = w_f (동일).** "2π 비"는 **같은 숫자를 두 식에 넣었을 때만** 나오는 형식적 비.
- **Claude 최종 결정 → Codex 채택.** 내 blind 결론(2π 차이)은 근접장 정규화를 놓친 잘못.
  - 슬라이드 11의 실제 결함: **K 정규화를 정의하지 않아**, 독자가 같은 K를 양쪽에 대입하면 허위 2π가 생김.
  - 정정: 제목의 "같은 물리량" 은 **유지**(맞음). 표 각주 추가 —
    "선3의 K 는 선1의 `K_I/√(2π)`. 이 환산에서 `Y = w_f`. 두 논문의 K 에 같은 수치를 대입하지 말 것. 단위는 모두 J/m² = Pa·m."
  - LEFM 관계 `G_c = K_IC²/E'` 를 함께 쓰면 (평면응력) `Y = w_f = G_c/2π` — round9에서 유지한 단서와 정합.
    선3 본문의 "Y ≡ G_c" 설명과 정규화된 응력장을 **동시에 무조건 참으로 둘 수 없음**(2π 불일치).

### D-09 [슬라이드 10] "Y 의 유도" 에 선1 식 사용
- **Claude blind (C10-03)**: 선1 유도를 선3 결과로 라벨 → 슬라이드 11과 **모순**.
- **Codex 이견 (R10-03)**: "반드시 물리적으로 모순"은 아님. `Y = w_f` 와 K 환산을 명시하면 Y 설명으로 사용 가능. 정확히는 **출처·기호·임계조건 미비**.
- **Claude 최종 결정 → Codex 채택 (모순 → 미비로 등급 하향).**
  - image13/14/15 는 선1 식(4)~(7)의 정규화. "선3 식(3) 직접 대입"이라고 부르면 부정확.
  - 정정: 제목을 "선1의 균열 극한 유도 + 선3 기호(Y) 환산" 으로, 그리고
    "선3은 근접장에서 2π를 빼고 K 를 정의(식3) → 같은 절차 → `Y = K²/E`(식4). 두 결과는 K 정규화만 다르고 물리량은 같다" 를 한 줄 추가.
  - image13 `w_f = K_I²/(2πE)` 는 `K_I` 가 임계임을 밝혀야 재료상수. 임의 하중은 `Q = K_I²/(2πE)`, 파괴 시 `w_f = K_IC²/(2πE)` (Codex R10-15).

---

## 3. Codex 추가 지적 — Claude 채택

| ID | 슬라이드 | 내용 |
|---|---|---|
| **D-10** | 10 | **"홀은 특성이 없어 FE로 구배를 뽑는다" 는 그대로면 오류.** 선1 Case 3(무한판 원형 홀)은 식(8)~(13)으로 **해석적** 처리 (`σ_θ=3σ₀`, `|σ_θ'|=7σ₀/R` → `Q = 27σ₀²R/(14E)`). FE는 Case 4(유한판)에서만. 정정: "홀에는 균열의 `r^{-1/2}` 특이장이 없어 소거식을 바로 못 쓴다. 유한판 사례4에서는 FE로 구배 평가" (Codex R10-14). |
| **D-11** | 6 | image6 "응력 분포가 넓을수록 Q 커진다" 에 "같은 국소응력·E에서" 단서 추가. 선형탄성 비례하중에서 `Q → λ²Q` 이므로 "하중 키우면 임계값 도달" 설명 자체는 이 조건에서 맞음 (Codex R10-11). 균일응력(구배 0)은 분수식이 0/0 → 부등식 `σ³ ≥ 2EY|σ'|` 형태로 판단, 구배조건 자동충족 → 응력조건만 남음 (Codex R10-12, 선1 Case 1). |
| **D-12** | 13 | "시편 6개 / 나머지 5개" → "6개 형상 케이스 / 보정에 쓰지 않은 5개 케이스". **보정점 = H=100 mm, a/H=0.1** — 이 점의 예측·실험 일치는 강제된 것이라 독립 검증 성과로 세면 안 됨. H=100 mm·a/H=0.3 불일치를 저자도 보고 → 발표에서 이 예외 누락 금지 (Codex R10-16, R10-17). Fig.10 정규화 기준길이가 H라고 원문에 명시돼 있지 않음 — 재현 시 확인 항목 (round9 R9-08 재확인). |
| **D-13** | 4·6 | "두 조건이 동시에 성립해야 파괴" 는 **"이 기준에서는"** 으로 한정 (보편 법칙 단정 회피). "그 점의 힘" → "그 점의 응력", "얼마나 세게 눌리나" → "단위면적당 작용하는 힘"(인장 포함) (Codex R10-09). |
| **D-14** | 10 | "이 한 번의 소거가 … 수학적 정리" → "이상적 Mode I 균열 근접장과의 정합성을 보인다"로 제한. 임의 노치·모드·CZM/FFM 전체 포함 증명이 아님 (Codex R10-13). |
| **D-15** | 5 | image4 `σ_i` → 선1의 국소응력 `σ_l` (소문자 ℓ). 최대수직/최대전단 응력의 재료 대응을 명확히, σ_f 도 같은 척도로 정의 (Codex R10-05, #5). |
| **D-16** | 2~13 | `[n] Reference` 플레이스홀더를 실제 번호·서지로 교체 필요 (Codex #11). |
| **D-17** | 9 | `G = K_I²/E'` 에 "Mode I·등방·선형탄성" 조건과 E′ 정의 부기. Griffith(1921)·Irwin(1957) 을 현대 K,G 표기 원저자로 귀속하지 않도록 표현 주의 (Codex #10). |

---

## 4. 최종 브리핑 (사용자 보고본)
`J:\Desktop\공학\claude_code\2026_09_08_발표pptx_오류검토\발표pptx_오류검토_브리핑.html`
- 필수 4 (D-01 식 2KY / D-02 에너지밀도 지수 / D-08 슬11 K 정규화 각주 / D-09 슬10 라벨) +
  신규 필수 1 (D-10 홀 FE 서술) + 보완·표현 다수.
- **내 blind 분석의 자기정정 명시**: "2π 차이" (X) → "K 정규화만 다르고 물리량은 같다 (Y = w_f)" (O).

## 5. 범위 밖 / 미확인
- Karihaloo 2003 원 실험표·반복수 (출판사 403), 선3 웹 원문 (429) — 제공 스캔 이미지로만 검증.
- 시멘트 FE 원 코드가 실제 어떤 K 정의를 썼는지 — 이 자료로 확인 불가.
- pptx 전체 렌더링·배치·애니메이션·숨은 개체·발표자 노트 — 텍스트/수식 덤프 + 핵심 이미지 범위로 한정.
- Griffith 1921 / Irwin 1957 원문 전문 — 이번 미확인.

---

## 7. 후속 산출물 — 슬라이드 6·10 정정본 (2026-09-08)
사용자 요청으로 슬라이드 6·10을 원문에 맞춰 다시 그림.
- Artifact(링크): https://claude.ai/code/artifact/54a7e5ca-5d34-4969-a2d0-85bdb4d03aab
- 이미지: `round10/정정슬라이드/slide6.png`, `slide10.png` (1200px 슬라이드, PPT 삽입용)
- 통합 HTML: `round10/정정슬라이드/슬라이드6_10_정정_통합.html`, `2026_09_08_발표pptx_오류검토/`
- 슬라이드 6: 조건식 → Q 분해(σ_e²/2E × σ_e/|dσ_e/ds|) → 직관(그림) → 하중증가 → 균일응력, 순서 재배치. D-02·D-11·D-13 반영.
- 슬라이드 10: 선1·선3 유도 나란히, K_선3 = K_I/√(2π) → Y = w_f 명시, 홀은 무한판(해석식)/유한판(FE) 구분. D-08·D-09·D-10·D-14 반영.
