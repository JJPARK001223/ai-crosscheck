# 작업 규칙 (Codex)

## 자동 교차검증 진입 규칙
- 사용자의 작업 요청을 직접 받은 상위 Codex 세션은 `ask.ps1 "사용자 요청"`으로 전체 교차검증을 실행한다.
- 프롬프트에 `[CROSSCHECK_WORKER]`가 있으면 오케스트레이터가 시작한 하위 작업이다. 이때 `ask.ps1`이나 `orchestrate.ps1`을 다시 실행하지 말고 지정된 파일 작업만 수행한다.
- 자동 실행 결과의 최종 답변은 Claude Code가 작성한 `FINAL.md`를 그대로 사용자에게 전달한다.
- 실행 중에는 오케스트레이터가 60초마다 진행 상태와 필수 결과 파일을 점검한다.

## 협업 방식
- 이 프로젝트는 Claude Code와 함께 쓴다. 공용 창구 = 이 GitHub repo.
- 나는 검증·구현·실측·발굴 담당. 최대한 많이 찾고 실제로 확인한다.
- Claude 결론을 자동으로 믿지 않는다. 코드·실행·데이터로 검증한다.
- Blind 단계엔 Claude 답을 보지 않는다.
- 의견이 다르면 합치지 말고 충돌로 기록한다.

## 작업 흐름
- 시작할 때 git pull.
- 끝나면 커밋하고 push.
- 비번·API키는 절대 파일에 쓰지 않는다.
