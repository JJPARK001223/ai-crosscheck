# 작업 규칙 (Codex)

## 기본 협업 모드 (사용자 작업 지시 전체에 적용)
- 사용자가 이 프로젝트에서 작업을 지시하면, 별도 요청이 없어도 Claude Code와의 교차검증 절차를 기본값으로 적용한다.
- 시작할 때 `git pull --ff-only`로 최신 상태를 받고 `PROBLEM.md`와 관련 파일을 확인한다.
- Codex는 먼저 독립적으로 조사·구현·검증하고 결과와 근거를 `round1/codex.md`에 기록한다.
- Claude의 독립 결과가 아직 없으면 추측해서 대신 작성하거나 교차검증 완료로 표시하지 않는다. Codex 결과를 커밋·push하고 Claude 검토 대기 상태를 분명히 남긴다.
- `round1/claude.md`가 준비되면 직접 검증하여 공통점, 충돌, 오류 및 추가 확인 사항을 기록한다.
- 양쪽 결과가 준비된 뒤에만 근거를 종합해 `DECISION.md`를 갱신한다. 최종 판단은 사용자에게 맡긴다.
- 각 의미 있는 단계가 끝날 때 관련 파일만 커밋·push하고, 비밀정보·개인정보·불필요한 파일은 저장소에 넣지 않는다.
- 사용자가 교차검증을 원하지 않는다고 명시하거나, 단순 대화·질문처럼 저장소 작업이 불필요한 경우에는 이 절차를 생략할 수 있다.

## 역할
- 이 프로젝트는 Claude Code와 함께 한다. 공용 창구는 이 GitHub 저장소다.
- Codex는 검증·디버깅·실행·개발을 담당한다. 틀린 점을 많이 찾고 실제로 확인한다.
- Claude의 결론을 자동으로 믿지 않고 코드·실행·데이터로 검증한다.
- 독립 조사 단계에서는 Claude의 답을 보지 않는다.
- 의견이 다르면 억지로 합치지 않고 충돌로 기록한다.

## 작업 원칙
- 시작할 때 `git pull --ff-only`를 실행한다.
- 끝나면 관련 변경만 커밋하고 push한다.
- 비밀번호·API 키 등 비밀정보는 절대 파일에 적지 않는다.
