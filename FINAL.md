> 이 최종 답변의 작성자: **Claude Code** (Claude ↔ Codex 교차검증 후 종합)

# LEFM 초보자용 설명 — 단일 HTML 문서 완료

## 결과물

**`round1/lefm_초보자_설명.html`** — 브라우저로 바로 열리는 단일 HTML 파일 하나.
외부 파일·CDN·라이브러리·이미지 파일 의존 0개. 그림 3개는 문서 안의 `<canvas>` +
순수 자바스크립트로 그립니다.

출처는 요청대로 아래 두 PDF **본문만** 사용했습니다.

1. **[선행연구 1]** Y. W. Kwon, "Revisiting Failure of Brittle Materials,"
   *Journal of Pressure Vessel Technology*, **143**(6), 064503, Dec. 2021. DOI: 10.1115/1.4050989
2. **[선행연구 3]** Y. W. Kwon, E. K. Markoff, S. DeFisher, "Unified Failure Criterion
   Based on Stress and Stress Gradient Conditions," *Materials* **17**(3), 569, Jan. 2024.
   DOI: 10.3390/ma17030569

`K = σ√(πa)` 같은 교과서 표준식, Griffith 에너지평형 유도, 소규모 항복(SSY)·K-지배영역
설명은 이 두 논문 본문에 없고 참고문헌에만 있어 **다루지 않았습니다**(문서 상단에 그 한계를 명시).

## 문서 구성

| 절 | 내용 |
|---|---|
| 1 | LEFM 한 줄 정의 (선형탄성 + 날카로운 균열 + K + K=K_Ic 파손) |
| 2 | 왜 일반 강도 기준(σ_l ≥ σ_f)으로는 균열을 못 다루나 — 선단 응력이 무한대라서. [선1]의 국부응력 σ_l 과 [선3]의 유효응력 σ_e 구분 |
| 3 | **균열선단 응력집중과 K** — `σ_x = K/√(2πr)` ([선1] Eq.4), 응력구배 식 ([선1] Eq.5) + **그림 1·2** |
| 4 | **K 대 K_Ic 파손 판정** — `w_f = K²/(2πE)`, `K_Ic = √(2πE·w_f)` ([선1] Eq.6–7), [선3]의 `Y = K²/E` + **그림 3** + 두 논문의 2π 계수 차이 경고 |
| 5 | 적용 범위·한계 (날카로운 균열 전제, 구멍·둔한 노치·연성재는 별도 취급) |
| 6 | 핵심 수식 요약표 (식마다 [선1]/[선3] 출처 표기) |
| 7 | 결론 |

## 요청하신 두 시각화

- **균열선단 응력집중** (그림 1·2): `σ_x = K/√(2πr)` 를 실제로 계산해 그립니다.
  그림 1은 선형 축에서 `r → 0` 일 때 응력이 무한대로 발산하는 모습과 파손강도 σ_f 파선을,
  그림 2는 같은 곡선을 로그–로그 축으로 그려 **기울기 −1/2** (= "1/√r 특이성")를 보여줍니다.
- **K 대 K_Ic 파손 판정** (그림 3): 왼쪽은 K가 하중 P에 비례해 증가하다 K_Ic에 도달하는
  하중 P_f 에서 파손하는 그래프, 오른쪽은 하중 단계별 K/K_Ic 막대(낮음 0.4 / 중간 0.7 / 파손 1.0).

각 그림에는 자바스크립트가 꺼졌을 때 보이는 대체 설명과 스크린리더용 `aria-label` 을 넣었고,
`<script>` 끝에 식이 깨지지 않았는지 확인하는 `console.assert` 자가검사 3개를 두었습니다.

## 교차검증 결과 (Claude ↔ Codex)

Claude와 Codex가 각자 독립적으로 HTML을 만든 뒤 서로 검토했습니다. **두 결과 사이에
사실관계 충돌은 없었고**, 인용한 식·문장은 양쪽 다 두 PDF와 일치했습니다(claude는 PDF 직접
열람, codex는 `pdftotext` 재추출 + HTML 스크립트를 Node에서 실제 실행해 확인).

검토에서 나온 지적을 반영해 다음을 수정했습니다.

1. **`w_f` = "에너지방출률"** → "에너지방출률과 **같은 차원**". [선1]은 `w_f` 를 응력구배 조건에서
   나온 파손 임계값으로 도입하고 그 차원이 J/m² 라고만 합니다. 임계 에너지방출률과의 명시적
   등가성은 [선3]의 `Y` 에만 있습니다("equivalent to the critical energy release rate").
2. **출처 밖 기호 `G_c` 제거** → `Y` 로 통일 (두 PDF에 `G_c` 전개 없음).
3. **σ_l / σ_e 구분 추가** — [선3]의 유효응력 개념이 연성·이방성·복합재 확장의 근거임을 명시.
4. **2π 계수 차이** — [선1] `K²/(2πE)` 와 [선3] `K²/E` 를 섞어 쓰지 말라는 경고는 유지하되,
   원인을 단정하지 않고 두 식을 분리 표기(문서 내부 모순 제거).
5. **출처에 없는 재료명 삭제** ("PMMA" 등) → 두 논문이 든 예(경화 시멘트 페이스트)로 한정.
6. **"LEFM은 균열 전용"** 같은 절대적 표현 → "이 두 논문의 K–K_Ic 판정은 날카로운 균열에 적용".

자세한 공통 의견·충돌·근거는 `DECISION.md` 참조.

## 확인 필요

- 브라우저 렌더링 육안 확인은 이번 작업 환경에 브라우저가 없어 못 했습니다.
  **파일을 한 번 열어 그림 3개와 글자 겹침 여부를 봐 주세요.** (스크립트는 Node에서 실행 검증됨:
  예외 없음, 자가검사 통과.)
- 예시 수치(K = 1 MPa√m, E = 3 GPa, σ_f = 70 MPa, K_Ic = 1 MPa√m)는 곡선 **형태**를
  재현하기 위한 임의값이며 실제 재료값이 아닙니다(문서에 명시).
